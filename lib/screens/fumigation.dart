import 'package:flutter/material.dart';

class FumgnPage extends StatelessWidget {
  const FumgnPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Fumigation'),
      ),
      body: Container(
        color: Colors.black,
      ),
    );
  }
}
