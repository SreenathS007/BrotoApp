import 'package:flutter/material.dart';

class BanglrHub extends StatelessWidget {
  const BanglrHub({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Bangalore Hub'),
        backgroundColor: Colors.black,
      ),
    );
  }
}
